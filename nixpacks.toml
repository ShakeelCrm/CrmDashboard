[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = [
  "cd apps/api",
  "npx prisma generate",
  "pnpm build"
]

[start]
cmd = "cd apps/api && pnpm start"